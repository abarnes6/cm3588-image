[Unit]
# Gate startup on both mounts: without /persist Plex creates a fresh, empty
# identity in the stub directory on the root filesystem; without
# /srv/nfs/data a scheduled empty-trash purges every library entry.
RequiresMountsFor=/persist /srv/nfs/data

[Service]
EnvironmentFile=/etc/default/plexmediaserver-datadir
