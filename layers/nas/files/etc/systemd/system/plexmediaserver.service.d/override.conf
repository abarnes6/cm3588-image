[Unit]
# Gate startup on the array so a missing mount can never cause Plex to create
# a fresh, empty identity in the stub directory on the root filesystem.
RequiresMountsFor=/srv/nfs/data

[Service]
EnvironmentFile=/etc/default/plexmediaserver-datadir
