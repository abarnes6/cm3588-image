# Resolver for processes inside the qbt namespace. The host's /etc/resolv.conf
# points at systemd-resolved's 127.0.0.53 stub, which does not exist in here.
# Proton serves DNS in-tunnel at 10.2.0.1.
nameserver 10.2.0.1
