# smartd is the ONLY pre-failure warning for the disks behind /srv/nfs/data —
# nothing there degrades gracefully. -m needs a working mail path:
# configure a relay in /etc/msmtprc or nothing is delivered.
DEVICESCAN -d removable -n standby -m root -M exec /usr/share/smartmontools/smartd-runner
